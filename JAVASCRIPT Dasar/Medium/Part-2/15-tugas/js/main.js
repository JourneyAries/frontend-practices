//15. Menemukan Nilai yang Berada pada Posisi Tertentu
//Buat fungsi findAtIndex(arr, index) yang menerima array arr dan indeks index, dan mengembalikan nilai yang berada pada indeks tersebut.

function findAtIndex(arr, index) {
  return arr[index];
}

console.log(findAtIndex([10, 20, 30], 1));